#include<bits/stdc++.h>
using namespace std;
int main(){
    set<int> s;
    s.insert(1);
    s.insert(9);
    s.insert(8);
    s.insert(9);
    
    for(auto a:s){
        cout<<a<<" ";
    }
    // cout<<endl;
    // set<int, greater<int>> s1;
    // s1.insert(1);
    // s1.insert(9);
    // s1.insert(8);
    // s1.insert(9);
    // for(auto aryan:s1){
    //     cout<<aryan<<" ";
    // }
    
    //auto keyword
    // a pointer that is pointing to a collection of data, Now the data is stored where?
    // data is stored in s
    // a pointing to s
    // first it will point to first element then second element then so on and so forth


}
